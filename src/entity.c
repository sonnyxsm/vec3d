#include "entity.h"

void ent_create(entity_t *e, const char *path, Vector3 position, Vector3 rotation, Vector3 scale)
{
        if (!path) {

        }
}

void ent_fire_anim(entity_t *e, int action)
{

}
